<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Azone — Luxury Redefined</title>

  <style>
    :root {
      --bg: #0b0b0b;
      --fg: #f5f5f5;
      --muted: #b5b5b5;
      --gold: #c9a24d;
      --card: #141414;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Inter", Arial, sans-serif;
      background: var(--bg);
      color: var(--fg);
      line-height: 1.6;
      overflow-x: hidden;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    /* ANIMATIONS */
    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .fade {
      animation: fadeUp 1s ease forwards;
    }

    /* NAV */
    header {
      padding: 20px 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-size: 22px;
      letter-spacing: 3px;
      color: var(--gold);
      font-weight: 600;
    }

    nav a {
      margin-left: 20px;
      font-size: 14px;
      color: var(--muted);
    }

    nav a:hover {
      color: var(--gold);
    }

    /* HERO */
    .hero {
      min-height: 90vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 0 24px;
    }

    .hero h1 {
      font-size: 42px;
      max-width: 600px;
      margin-bottom: 16px;
    }

    .hero p {
      color: var(--muted);
      max-width: 520px;
      margin-bottom: 32px;
    }

    .btn {
      display: inline-block;
      padding: 14px 28px;
      border: 1px solid var(--gold);
      color: var(--gold);
      font-size: 14px;
      letter-spacing: 1px;
      transition: all 0.3s ease;
      width: fit-content;
    }

    .btn:hover {
      background: var(--gold);
      color: #000;
    }

    /* SECTIONS */
    section {
      padding: 80px 24px;
      max-width: 1100px;
      margin: auto;
    }

    section h2 {
      font-size: 28px;
      margin-bottom: 16px;
      color: var(--gold);
    }

    section p {
      color: var(--muted);
      max-width: 700px;
    }

    /* CARDS */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 24px;
      margin-top: 40px;
    }

    .card {
      background: var(--card);
      padding: 24px;
      border: 1px solid #1f1f1f;
      transition: transform 0.3s ease, border 0.3s ease;
    }

    .card:hover {
      transform: translateY(-6px);
      border-color: var(--gold);
    }

    .card h3 {
      margin-bottom: 10px;
      color: var(--fg);
    }

    .card p {
      font-size: 14px;
    }

    /* FOOTER */
    footer {
      padding: 40px 24px;
      text-align: center;
      border-top: 1px solid #1f1f1f;
      color: var(--muted);
      font-size: 13px;
    }

    footer span {
      color: var(--gold);
    }

    /* MOBILE */
    @media (max-width: 600px) {
      .hero h1 {
        font-size: 34px;
      }
    }
  </style>
</head>

<body>

  <header class="fade">
    <div class="logo">AZONE</div>
    <nav>
      <a href="#about">About</a>
      <a href="#collections">Collections</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <div class="hero fade">
    <h1>Luxury That Speaks Without Words</h1>
    <p>
      Azone is a modern luxury brand built for those who value design,
      precision, and quiet confidence.
    </p>
    <a class="btn" href="#collections">Explore Collection</a>
  </div>

  <section id="about" class="fade">
    <h2>About Azone</h2>
    <p>
      Azone blends minimalist aesthetics with premium craftsmanship.
      Every product is designed to feel exclusive, timeless, and refined —
      made for individuals who move differently.
    </p>
  </section>

  <section id="collections" class="fade">
    <h2>Our Collections</h2>

    <div class="grid">
      <div class="card">
        <h3>Men</h3>
        <p>Tailored essentials with sharp silhouettes and luxury fabrics.</p>
      </div>

      <div class="card">
        <h3>Women</h3>
        <p>Elegant designs balancing power, grace, and modern luxury.</p>
      </div>

      <div class="card">
        <h3>Accessories</h3>
        <p>Minimal pieces that elevate your presence effortlessly.</p>
      </div>
    </div>
  </section>

  <section id="contact" class="fade">
    <h2>Contact</h2>
    <p>
      Business inquiries & collaborations<br />
      <br />
      📩 contact@azone.com
    </p>
  </section>

  <footer>
    © 2026 <span>AZONE</span>. All rights reserved.
  </footer>

</body>
</html>
